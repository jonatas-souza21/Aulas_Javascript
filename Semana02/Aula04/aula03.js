let frutas = ['Acabaxi', 'Abacate', 'Amora', 'Banana'];

for (let contadorFrutas = 0; contadorFrutas < frutas.length; contadorFrutas += 1) {
    console.log(`Nome da fruta: ${frutas[contadorFrutas]}`);
}
